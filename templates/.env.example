# URL of the published app — the dev stage URL from the ODC portal
BASE_URL=https://your-tenant.outsystems.app/YourAppName

# Demo/test user. Used once by tests/auth.setup.ts to produce tests/.auth/user.json
APP_USER=
APP_PASSWORD=
